services:
  - type: web
    name: elite-honeypot-raw
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python simple_server.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
    autoDeploy: true